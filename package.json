{
  "name": "furigana-annotations",
  "version": "1.0.0",
  "description": "A library for parsing furigana annotations",
  "main": "dist/FuriganaParser.js",
  "types": "dist/FuriganaParser.d.ts",
  "repository": "https://github.com/atomicptr/furigana-annotations",
  "author": "Christopher Kaster <me@atomicptr.de>",
  "license": "MIT",
  "private": false,
  "devDependencies": {
    "typescript": "^4.5.4"
  },
  "scripts": {
    "build": "npm run build:lib && npm run build:browser",
    "build:lib": "tsc",
    "watch:lib": "tsc --watch",
    "build:browser": "tsc -p tsconfig.site.json",
    "watch:browser": "tsc --watch -p tsconfig.site.json"
  },
  "files": [
    "/dist"
  ]
}
